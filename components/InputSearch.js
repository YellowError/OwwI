import React from "react";
const InputSearch = ({ placeholder }) => {
  return (
    <>
      <input type="search" className="inputSearch" placeholder={placeholder} />
    </>
  );
};
export default InputSearch;
