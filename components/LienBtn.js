import React, {useEffect} from React;

const LienBtn=({value, cheminBtn ="#"}) =>{

    return ( 
    <Link href={cheminBtn, value }>
    <div  className="mx-auto flex flex-col my-20xp max-w-sm  " >
        <button className="bg-blue-400 hover:bg-blue-300 text-white m-1 px-5 py-3 font-bold rounded"
        value={}
        >

        </button>
    </div>

    </Link>
)
} 

export default  LienBtn